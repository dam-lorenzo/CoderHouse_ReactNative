import { StyleSheet, Text } from "react-native"


const Title = () => {
    return (
        <Text style={styles.title}>Entrega 5</Text>
    )
}

const styles = StyleSheet.create({
    title: {
        padding: 20,
        marginTop: 50
    },
})

export default Title